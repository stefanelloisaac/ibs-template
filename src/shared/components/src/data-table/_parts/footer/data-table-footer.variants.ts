import { tv } from 'tailwind-variants';

export const dataTableFooterVariants = tv({
  slots: {
    root: 'font-medium',
    cell: 'p-4 align-middle bg-muted',
  },
  variants: {},
  defaultVariants: {},
});
