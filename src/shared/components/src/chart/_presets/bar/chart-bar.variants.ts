import { tv } from 'tailwind-variants';

export const chartBarVariants = tv({
  slots: {
    bar: 'stroke-none opacity-80 transition-opacity duration-150',
  },
  variants: {},
  defaultVariants: {},
});
