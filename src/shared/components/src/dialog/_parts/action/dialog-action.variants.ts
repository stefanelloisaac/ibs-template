import { tv } from 'tailwind-variants';

export const dialogActionVariants = tv({
  slots: {
    root: 'col-start-2 row-span-2 row-start-1 self-start justify-self-end flex items-center gap-2 text-sm',
  },
  variants: {},
  defaultVariants: {},
});
