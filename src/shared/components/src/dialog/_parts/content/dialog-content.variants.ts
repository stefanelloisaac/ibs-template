import { tv } from 'tailwind-variants';

export const dialogContentVariants = tv({
  slots: {
    root: 'px-2 py-4 border-y overflow-y-auto flex-1 min-h-0',
  },
  variants: {},
  defaultVariants: {},
});
