import { tv } from 'tailwind-variants';

export const buttonLinkVariants = tv({
  slots: {
    root: 'border border-primary text-primary underline-offset-4 hover:underline',
  },
  variants: {},
  defaultVariants: {},
});
